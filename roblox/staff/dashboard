<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Staff Dashboard</title>
  <script src="https://cdn.jsdelivr.net/npm/appwrite@13.0.0"></script>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background-color: #1e1e2f;
      color: white;
    }

    .intro {
      position: fixed;
      top: 0; left: 0; width: 100%; height: 100%;
      background-color: #121222;
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 1000;
      animation: fadeOut 1s ease 2.5s forwards;
    }

    @keyframes fadeOut {
      to {
        opacity: 0;
        visibility: hidden;
      }
    }

    .dashboard {
      display: none;
      padding: 20px;
    }

    .header {
      display: flex;
      justify-content: flex-end;
      align-items: center;
      padding: 20px;
      background-color: #2a2a3b;
    }

    .profile {
      display: flex;
      align-items: center;
      gap: 10px;
      position: relative;
    }

    .profile img {
      width: 40px;
      height: 40px;
      border-radius: 50%;
    }

    .username {
      font-weight: bold;
    }

    .settings-icon {
      width: 24px;
      height: 24px;
      cursor: pointer;
    }

    .dropdown {
      position: absolute;
      top: 50px;
      right: 0;
      background-color: #333;
      border: 1px solid #444;
      display: none;
      padding: 10px;
      border-radius: 5px;
      z-index: 999;
    }

    .dropdown a {
      color: white;
      text-decoration: none;
      display: block;
      padding: 5px 10px;
    }

    .dropdown a:hover {
      background-color: #444;
    }
  </style>
</head>
<body>
  <div class="intro" id="intro">
    <h1 id="welcome-msg">Welcome Back!</h1>
  </div>

  <div class="dashboard" id="dashboard">
    <div class="header">
      <div class="profile" id="profile-section">
        <span class="username" id="username">User</span>
        <img id="avatar" src="" alt="Avatar">
        <img src="/roblox/staff/settingsstaff.png" class="settings-icon" id="settingsBtn" alt="Settings">
        <div class="dropdown" id="dropdown">
          <a href="#" id="logoutBtn">Logout</a>
        </div>
      </div>
    </div>

    <div class="content">
      <h2>Staff Dashboard</h2>
      <p>More features coming soon!</p>
    </div>
  </div>

  <script>
    const client = new Appwrite.Client()
      .setEndpoint('https://fra.cloud.appwrite.io/v1')
      .setProject('rlrs');

    const account = new Appwrite.Account(client);

    account.get().then(user => {
      const username = user.name || "Unknown";
      const avatarUrl = user.prefs?.avatar_url || `https://api.dicebear.com/6.x/initials/svg?seed=${username}`;

      document.getElementById("username").innerText = username;
      document.getElementById("avatar").src = avatarUrl;
      document.getElementById("welcome-msg").innerText = `Welcome Back, ${username}!`;

      // Show dashboard after intro animation
      setTimeout(() => {
        document.getElementById("dashboard").style.display = "block";
      }, 2500);
    }).catch(error => {
      console.error("Session not found", error);
      window.location.href = "/roblox/staff/login";
    });

    // Dropdown toggle
    const settingsBtn = document.getElementById("settingsBtn");
    const dropdown = document.getElementById("dropdown");

    settingsBtn.addEventListener("click", () => {
      dropdown.style.display = dropdown.style.display === "block" ? "none" : "block";
    });

    // Logout handler
    document.getElementById("logoutBtn").addEventListener("click", (e) => {
      e.preventDefault();
      account.deleteSession("current").then(() => {
        window.location.href = "/roblox/staff/login";
      });
    });
  </script>
</body>
</html>
